# API Tokens

REPLICATE_API_TOKEN=

# Google OAuth Configuration

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Database Configuration

# Make sure to replace the placeholders with your actual database connection details

DATABASE_URL="postgres://USERNAME:PASSWORD@HOST:PORT/DATABASE"

# NextAuth Configuration

NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Stripe Configuration

STRIPE_SECRET=

# Note:

# - Ensure you replace USERNAME, PASSWORD, HOST, PORT, and DATABASE with your actual database credentials.

# - For security reasons, never commit your real secrets (like API tokens, client secrets, database URLs) to version control.
